# Hand API

`Hand` is the high-level API for joint commands, named grasps, and emergency stop behavior.

Core methods:

- `Hand.mock()`
- `read_joints()`
- `move_joints()`
- `move_to_grasp()`
- `open()`
- `stop()`
